import asyncioimport loggingfrom alembic.config import Configfrom alembic import commandfrom app.core.config import settingslogger = logging.getLogger(__name__)async def run_migrations():    """Запуск миграций Alembic асинхронно"""    try:        logger.info("🔄 Запуск миграций базы данных...")        # Конфигурация Alembic        alembic_cfg = Config("alembic.ini")        alembic_cfg.set_main_option("sqlalchemy.url", settings.database_url)        # Запуск миграций в отдельном потоке для совместимости с async        await asyncio.to_thread(command.upgrade, alembic_cfg, "head")        logger.info("✅ Миграции успешно применены")    except Exception as e:        logger.error(f"❌ Ошибка при выполнении миграций: {e}")        # Не прерываем запуск приложения из-за миграций        logger.warning("⚠️ Продолжаем запуск без миграций")async def init_alembic():    """Простая инициализация миграций"""    try:        await run_migrations()    except Exception as e:        logger.error(f"❌ Ошибка инициализации Alembic: {e}")        logger.warning("⚠️ Продолжаем запуск без миграций")